services:
  - type: web
    name: piping-design-mcp
    runtime: python
    buildCommand: pip install -r piping_mcp/requirements.txt
    startCommand: uvicorn piping_mcp.server:app --host 0.0.0.0 --port $PORT
